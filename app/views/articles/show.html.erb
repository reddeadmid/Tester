<%= render "mynavbar", article: @article %>

<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<button class="btn btn-dark" type="button"><%= link_to "Edit", edit_article_path(@article) %></button>
<button class="btn btn-dark" type="button"><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li></button>

<br><br>

<h2>Comments</h2>
<%= render @article.comments %>

<%= render 'comments/form' %>
